<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.im.repo.FinancialOrderMapper">

    <select id="findByOrderNo" resultType="com.example.im.domain.FinancialOrder">
        SELECT * FROM financial_order WHERE order_no = #{orderNo} LIMIT 1
    </select>

    <select id="findByCustomerIdOrderByIdDesc" resultType="com.example.im.domain.FinancialOrder">
        SELECT * FROM financial_order WHERE customer_id = #{customerId} ORDER BY id DESC
    </select>

    <select id="findByStatus" resultType="com.example.im.domain.FinancialOrder">
        SELECT * FROM financial_order WHERE status = #{status} ORDER BY id ASC
    </select>

</mapper>