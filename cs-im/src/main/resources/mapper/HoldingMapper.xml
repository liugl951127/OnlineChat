<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.im.repo.HoldingMapper">
    <select id="findByCustomerId" resultType="com.example.im.domain.Holding">
        SELECT * FROM holding WHERE customer_id = #{customerId} ORDER BY id DESC
    </select>
    <select id="findByCustomerIdAndStatus" resultType="com.example.im.domain.Holding">
        SELECT * FROM holding
        WHERE customer_id = #{customerId} AND status = #{status}
        ORDER BY id DESC
    </select>
</mapper>